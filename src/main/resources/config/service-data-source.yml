spring:
  datasource:
    dynamic: true
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&tinyInt1isBit=false
      username: testUser
      password: Tu123456
      read-only: false
      #连接超时时长
      connection-timeout: 300000000
      #空闲连接超时时长
      idle-timeout: 600000
      #最大生命周期，0不过期
      max-lifetime: 0
      #最大连接数量
      maximum-pool-size: 5